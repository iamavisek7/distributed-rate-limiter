# distributed-rate-limiter
What problems it solves: Protects your service from overload and cascading failures.  Prevents abuse (credential stuffing, scraping, brute force).  Ensures fairness across users/tenants.  Cost control (e.g., paid APIs, LLM calls).  Improves reliability by smoothing traffic spikes.
